name: Cache Workflow

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-13ffe66

      - name: prime-cache-13ffe66
        run: echo "Cache hit: ${{ steps.cache.outputs.cache-hit }}"
